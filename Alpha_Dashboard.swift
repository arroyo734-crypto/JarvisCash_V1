import SwiftUI`n`nstruct AlphaDashboardView: View {`n    @StateObject private var monitor = HighVolatilityMonitor()`n    @State private var traderStatus: String = "IDLE"`n    `n    var body: some View {`n        VStack(spacing: 25) {`n            Text("JARVIS 2.0: MAIN FRAME")`n                .font(.system(size: 12, weight: .bold, design: .monospaced))`n                .foregroundColor(.green)`n            `n            VStack {`n                Text("POLYMARKET ALPHA")`n                    .font(.caption)`n                    .foregroundColor(.gray)`n                `n                Text("$\(monitor.currentPrice, specifier: "%.2f")")`n                    .font(.system(size: 40, weight: .heavy, design: .rounded))`n                    .foregroundColor(monitor.isVolatile ? .red : .primary)`n                `n                if monitor.isVolatile {`n                    Text("?? HIGH VOLATILITY DETECTED")`n                        .font(.caption.bold())`n                        .foregroundColor(.red)`n                        .padding(5)`n                        .background(Color.red.opacity(0.2))`n                        .cornerRadius(5)`n                }`n            }`n            .padding()`n            .background(RoundedRectangle(cornerRadius: 15).fill(Color.secondary.opacity(0.1)))`n`n            HStack {`n                Circle()`n                    .fill(monitor.isVolatile ? Color.green : Color.gray)`n                    .frame(width: 10, height: 10)`n                Text("15 PRO TRADER: \(traderStatus)")`n                    .font(.system(size: 10, weight: .bold))`n            }`n`n            Button(action: {`n                monitor.checkForHighVolatility()`n                if monitor.isVolatile {`n                    traderStatus = "SIGNAL SENT"`n                }`n            }) {`n                Text("REFRESH MARKET ALPHA")`n                    .font(.headline)`n                    .foregroundColor(.white)`n                    .frame(maxWidth: .infinity)`n                    .padding()`n                    .background(Color.blue)`n                    .cornerRadius(12)`n            }`n        }`n        .padding()`n    }`n}
